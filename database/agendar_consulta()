from datetime import datetime

data = input("Data (dd/mm/aaaa): ")

data_consulta = datetime.strptime(data, "%d/%m/%Y")

if data_consulta < datetime.now():
    print("Não é possível agendar consulta no passado")
    return
#
cursor.execute(
    "SELECT * FROM pacientes WHERE cpf=?",
    (cpf,)
)

if not cursor.fetchone():
    print("Paciente não encontrado")
    return
